# Compaction Log - 14032026160543

## Cumulative History

### Session 13-03-2026 23:41 - Step 1 Data Pipeline Completed
- Initialized `tusharneje-dev/` repository, configured `main`, and set remote to `https://github.com/tusharneje-07/tusharneje-dev`.
- Added `.gitignore` entries for `node_modules/`, `Data Collection/`, `*.log`, `dist/`, `.cache/`, plus common local/system files.
- Created core folder structure with `.compaction/`, `data/`, and `assets/` subfolders for projects, certificates, research, hackathons, extracurricular, blogs.
- Parsed all source inputs from resume text, old portfolio site, project text files, course certificates, activity media, and research papers.
- Generated `data/` JSON files: `personal.json`, `experience.json`, `skills.json`, `projects.json`, `certificates.json`, `activities.json`, `research.json`, `blogs.json`.
- Converted PDFs to optimized images for certificates and research pages; downloaded profile image; documented missing screenshot handling.

### Session 14-03-2026 00:23 - Main Site Pages and Features
- Built/pushed `index.html`, `blog.html`, `show_resource.html`, then `blog-template.html` with dynamic rendering and markdown-related UX.
- Added certificates section with filters/show-more and navbar links.

### Session 14-03-2026 12:00 - Final Polish and Content Corrections
- Added activities section to `index.html` based on `data/activities.json`.
- Added favicon references to all main pages and created `assets/favicon.svg`.
- Rewrote `data/certificates.json` with corrected certificate metadata.
- Added `sitemap.xml` and `robots.txt`.

### Session 14-03-2026 15:43 - Compaction Understanding Update
- Read and consolidated previous compaction memory files.
- Verified required `.gitignore` exclusions were present.

### Session 14-03-2026 15:48 - Developer-Style Redesign Continuation
- Shifted design language to stronger developer aesthetic: dev-style typography, command-line branding, terminal card treatment, improved hero and mobile nav panel.

### Session 14-03-2026 15:54 - Hero/Mobile/Background/Hover Refinement
- Made hero profile block desktop-only (`hidden lg:block`) so mobile hero remains content-first.
- Added richer layered hero background (`.hero-aurora`, `.hero-circuit`, grid texture).
- Added profile hover polish (lift/shadow/border/image motion).
- Validated on `http://127.0.0.1:5500/index.html` in desktop + mobile emulation.

### Session 14-03-2026 15:56 - Scroll Cue Removal
- Removed hero scroll indicator UI and related CSS completely from `index.html`.

## Session 14-03-2026 16:05 - Skill Icons + Hero Cipher Animation + Creative Card Hover Upgrade

### User requests addressed
- Show SVG icon for each tech skill from external icon providers (no generated SVG).
- Replace static hero chips with encrypted/decrypted rotating text animation driven by `personal.json` (no hardcoded text in page logic).
- Make profile card hover animation more creative than simple zoom.

### Changes made
- Updated `data/personal.json` with new configurable hero animation block:
  - `hero_cipher.lines` (3 rotating lines)
  - `hero_cipher.charset`
  - `hero_cipher.timings` (`decrypt_step_ms`, `hold_ms`, `scramble_step_ms`, `between_line_ms`)
- Replaced static hero chip row with dynamic cipher UI (`#hero-cipher-text`) in `index.html`.
- Implemented `startHeroCipherAnimation()` in `index.html`:
  - Reads lines + charset + timing config from `personal.json`
  - Performs decrypt-in and scramble-out cycle continuously
  - No hardcoded rotating text content in HTML/JS logic
- Added SVG tech icons to all skills cards using Devicon CDN URLs (provider-based external SVGs), mapped by skill name.
- Enhanced skill card structure with icon container and consistent icon sizing/styling.
- Upgraded profile card hover animation with creative layered motion:
  - 3D tilt + lift + deeper shadow
  - Conic halo sweep animation
  - Light sheen pass across card
  - Subtle image micro-parallax on hover

### Validation
- Reloaded and tested `http://127.0.0.1:5500/index.html` in browser tooling.
- Confirmed hero cipher text animates and rotates.
- Confirmed skills section displays SVG icons from provider URLs.
- Confirmed profile hover now includes multi-layer creative animation.

---

This file is the latest cumulative memory snapshot. Future sessions should append new progress by creating the next timestamped `.compact_DDMMYYYYHHMMSS.md` with all prior history plus new updates.
