# Compaction Log — 14032026002300

## Session Summary — March 14, 2026

### What was accomplished this session:

#### 1. Committed & pushed all main pages (commit `10863f6`)
- `index.html` (~1044 lines) — Hero, About, Experience, Skills, Projects, Research, Contact
- `blog.html` — Blog listing with Coming Soon empty state
- `show_resource.html` — Fullscreen certificate/resource viewer

#### 2. Built `blog-template.html` (commit `df30517`)
- Individual blog post template reading from `blogs.json` via `?id=` URL param
- **Reading progress bar** (amber top bar updating on scroll)
- **Navbar/footer/theme/hamburger** parity with all other pages
- **Skeleton loaders** during fetch, **Not Found** state if ID invalid/missing
- **Prose styling** for markdown-rendered blog body (h1–h4, blockquote, code, pre, table, images, links)
- **Lightweight client-side markdown renderer** (fenced code blocks, headings, bold/italic/strikethrough, blockquote, lists, links, images, HR)
- **Copy-to-clipboard** buttons on each fenced code block
- **Table of Contents sidebar** (desktop, sticky, auto-built from h2/h3, active heading highlight via IntersectionObserver)
- **Reading time** estimate (200 wpm)
- **Share** button (native Web Share API fallback to clipboard copy)
- **Social share links** (X/Twitter, LinkedIn)
- **Open Graph meta tags** set dynamically per post
- **Breadcrumb** navigation (Home → Blog → Post)
- **Tags** chips in header and footer of post

#### 3. Added Certificates section to `index.html` (commit `df30517`)
- New section `#certificates` inserted between Research and Contact
- **Category filter tabs**: All, Programming, AI/ML, Development, Aptitude, Soft Skills, Leadership, Internship, Networking, Professional
- **Grid of 32 certificate cards** (5-column responsive grid, 3:2 aspect ratio)
- Each card shows certificate image thumbnail + name + issuer
- Cards link to `show_resource.html?cert=<id>` for fullscreen view
- **Show More** button — initially shows 10 per filter, reveals rest on demand
- Skeleton loaders while `certificates.json` loads
- Navbar updated: added "Certs" link (desktop) and "Certificates" link (mobile) pointing to `#certificates`

---

## Current State

### Pages (all built, committed, pushed):
- `index.html` ✅ — all 8 sections + certificates section
- `blog.html` ✅ — listing with empty state
- `blog-template.html` ✅ — individual post renderer
- `show_resource.html` ✅ — fullscreen cert viewer

### Data files (all in `data/`):
- `personal.json`, `experience.json`, `skills.json`, `projects.json`
- `certificates.json` (32 certs), `activities.json`, `research.json`
- `blogs.json` (empty array — Coming Soon)

### Assets:
- `assets/me.webp` — profile photo
- `assets/resume.pdf` — placeholder (user should replace)
- `assets/projects/*/` — 6 project screenshot folders
- `assets/certificates/` — 32 .webp files
- `assets/research/*/` — 3 research paper image folders
- `assets/hackathons/` — 12 .webp, `assets/extracurricular/` — 3 .webp

### Git log:
- `c19e796` — chore(data): initialize data pipeline, json files, and asset structure
- `10863f6` — feat(site): build all main pages — hero, about, experience, skills, projects, research, contact, blog, resource viewer
- `df30517` — feat(site): add blog-template.html and certificates section to index

---

## What remains (optional enhancements):
- `blog-template.html` will become fully functional once blog posts are added to `blogs.json`
  - Each post needs: `id`, `title`, `excerpt`, `date`, `tags`, `content` (markdown), optionally `cover`
- Activities section (hackathons) — `activities.json` has data but no UI section yet
- Replace placeholder `resume.pdf` with real file
- SEO: add `sitemap.xml`, `robots.txt`, favicon
- Replace generic NPTEL certificate names with real course names in `certificates.json`
