# Compaction Log - 14032026165447

## Cumulative History

### Prior Progress (Condensed)
- Portfolio repository initialized and populated with structured data files, assets, and core pages.
- Main site sections, blog pages, certificates, activities, and research views implemented.
- Multiple visual redesign passes performed for a developer-oriented style.
- Hero copy and cipher animation made data-driven via `data/personal.json`.
- Skills cards redesigned multiple times per feedback (square, minimal, denser, reduced whitespace).
- Skill icons moved to JSON (`data/skills.json` `icon_url`) to remove hardcoded icon mapping.

### Recent Design + UX Changes Before This Snapshot
- Certificates and Activities sections were redesigned to reduce performance bottlenecks:
  - Removed image thumbnails from list cards.
  - Added lightweight text-first cards.
  - Introduced popup modal preview (`#evidence-modal`) that loads image only on click.
  - Converted certificate cards from opening new tabs to opening in modal preview.
  - Added the same modal preview flow to hackathon/extracurricular cards.
- Certificates card style refined:
  - Removed the top orange line.
  - Made issuer label (NPTEL/Government/etc.) orange.
  - Made date capsule orange-styled.

## Session 14-03-2026 16:54 - Footer Build Story Page + Layout Tightening + Capsule Alignment

### New feature: OpenCode build-story page
- Added footer line in `index.html`:
  - `Designed by tusharneje with ❤️ and OpenCode`
  - `OpenCode` now links to `howibuild.html`.
- Created `howibuild.html`:
  - Shows complete prompt history used for the build.
  - Includes links to OpenCode website and repository.
  - Fetches and renders prompt text files from local copied prompt directory.
- Copied prompt source files into project (preserving structure):
  - `OpenCode Prompts/0.txt`
  - `OpenCode Prompts/1.txt`

### Experience section alignment fix
- Fixed award card year capsule placement in `index.html`:
  - Ensured content block uses full width and capsule stays right-aligned inside card.

### Navbar + Hero spacing reduction
- Reduced extra top spacing:
  - Navbar container height reduced (`h-16` -> `h-14`).
  - Hero top padding reduced (`pt-20` -> `pt-16`).
  - Hero vertical padding reduced (`py-16 md:py-24` -> `py-12 md:py-20`).

### Validation
- Verified on `http://127.0.0.1:5500/index.html`:
  - Hero/nav spacing is tighter.
  - Experience awards tab year capsule sits on the right within card.
  - Certificate cards reflect orange label + orange date capsule, and no top orange line.
  - Certificate and activities previews open in popup modal.
- Verified on `http://127.0.0.1:5500/howibuild.html`:
  - Prompt files load and display.
  - OpenCode link and repository link are present.

---

This file is the latest cumulative memory snapshot. Future sessions should append new progress by creating the next timestamped `.compact_DDMMYYYYHHMMSS.md` with all prior history plus new updates.
