# Compaction Log - 14032026162143

## Cumulative History

### Previous Progress (Condensed)
- Repo initialized, structured, and data pipeline completed.
- Main portfolio pages built and polished (hero, blog pages, activities, certificates, SEO files).
- Multiple design iterations applied for developer-style visuals and mobile behavior.
- Hero cipher animation made fully config-driven from `data/personal.json`.
- Skills icons moved to JSON (`data/skills.json` with `icon_url`) to avoid hardcoded icon mapping.
- Skills cards redesigned iteratively based on user feedback.

## Session 14-03-2026 16:21 - Skills Cards Changed to Squares (Minimal)

### User request
- Completely change card design.
- Use square cards instead of rectangles.
- Keep only icon, name, and percentage.
- No border on each card.

### Changes made in `index.html`
- Reworked `.skill-card` to square layout using `aspect-ratio: 1 / 1`.
- Removed card borders and simplified styling to a clean minimal surface.
- Removed extra card elements (no proficiency label/progress bar).
- Updated card markup to include only:
  - icon (`skill-icon-wrap` + `skill-icon`)
  - skill name (`skill-name`)
  - percentage (`skill-percent`)
- Adjusted responsive sizing for icons/text on mobile.

### Validation
- Reloaded and checked `http://127.0.0.1:5500/index.html`.
- Confirmed cards render as square minimal tiles with only icon, name, and percentage.

---

This file is the latest cumulative memory snapshot. Future sessions should append new progress by creating the next timestamped `.compact_DDMMYYYYHHMMSS.md` with all prior history plus new updates.
