# Compaction Log - 14032026161136

## Cumulative History

### Session 13-03-2026 23:41 - Data Pipeline and Base Setup
- Repository initialized and remote configured.
- Core project structure and asset folders created.
- Source portfolio/resume/project/activity/research data parsed.
- Initial JSON data files generated in `data/`.

### Session 14-03-2026 00:23 - Main Site Build
- Main pages built: `index.html`, `blog.html`, `blog-template.html`, `show_resource.html`.
- Certificates section and blog template features completed.

### Session 14-03-2026 12:00 - Polish and SEO Basics
- Activities section added.
- Favicon integrated across pages and `assets/favicon.svg` created.
- `data/certificates.json` corrected with real certificate details.
- `sitemap.xml` and `robots.txt` added.

### Session 14-03-2026 15:43 to 16:08 - Design Iterations and Data-Driven Hero/Icons
- Switched to stronger developer-centric visual language.
- Refined mobile behavior (hero profile hidden on mobile), hero backgrounds, and profile hover animation.
- Removed scroll hint UI by request.
- Added encrypted/decrypted rotating hero text driven by `data/personal.json` (`hero_cipher`).
- Added skill SVG icons from external providers.
- Removed hardcoded icon mapping from `index.html`; moved icon URLs into `data/skills.json` (`icon_url` per skill).

## Session 14-03-2026 16:11 - Creative Skills Card Redesign

### User request
- Existing skills card design felt too plain; requested a more creative card style.

### Changes made in `index.html`
- Redesigned skill cards with a new visual style:
  - Layered gradient card surface with subtle ambient glow.
  - Angled top-right accent corner.
  - Hover sheen sweep animation across each card.
  - Stronger hover lift/shadow/border emphasis.
  - Monospace percentage badge pill for better scanability.
  - Upgraded progress track/fill with glow and cleaner thickness.
- Updated skills rendering markup:
  - Card now uses `--skill-level` custom property.
  - Added dedicated classes: `.skill-percent`, `.skill-progress`, `.skill-progress-fill`.
  - Preserved JSON-driven icon usage (`s.icon_url`) and non-hardcoded architecture.

### Validation
- Reloaded and reviewed `http://127.0.0.1:5500/index.html`.
- Confirmed cards render with new creative styling and hover behavior.

---

This file is the latest cumulative memory snapshot. Future sessions should append new progress by creating the next timestamped `.compact_DDMMYYYYHHMMSS.md` with all prior history plus new updates.
