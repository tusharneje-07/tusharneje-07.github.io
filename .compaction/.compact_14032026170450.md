# Compaction Log - 14032026170450

## Cumulative History

### Prior Progress (Condensed)
- Full portfolio + blog static website implemented with JSON-driven content and organized assets.
- Major sections (hero, about, experience, skills, projects, research, certificates, activities, contact) built and iteratively redesigned.
- Certificates and activities were optimized to avoid heavy-scroll jank:
  - Thumbnail-heavy cards replaced by lightweight text-first cards.
  - On-click popup preview modal added so images load only on demand.
- Footer attribution and `howibuild.html` page added with copied OpenCode prompt files.
- Certificate card visual refinements applied (removed top line, orange kicker/date capsule).
- Hero/nav spacing tightened and experience awards year capsule alignment fixed.

## Session 14-03-2026 17:04 - Hero Title Size Adjustment

### User request
- Hero title looked too large after switching to the heavier display font style.

### Changes made
- Reduced hero heading typography scale in `index.html`:
  - `font-size` changed from `clamp(2.6rem,8vw,5.6rem)` to `clamp(2.2rem,6.4vw,4.7rem)`.
  - Slightly softened tracking and line-height for better balance.

### Validation
- Reloaded `http://127.0.0.1:5500/index.html` and confirmed the heading now fits the hero composition without overpowering the layout.

---

This file is the latest cumulative memory snapshot. Future sessions should append new progress by creating the next timestamped `.compact_DDMMYYYYHHMMSS.md` with all prior history plus new updates.
