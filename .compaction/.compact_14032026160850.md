# Compaction Log - 14032026160850

## Cumulative History

### Session 13-03-2026 23:41 - Data Pipeline Foundation
- Repository and base structure initialized.
- Source data parsed from resume, prior site content, projects, certificates, activities, and research files.
- Core JSON files created in `data/` and assets organized.

### Session 14-03-2026 00:23 - Main Pages and Core Features
- Main pages built and pushed (`index.html`, `blog.html`, `show_resource.html`, `blog-template.html`).
- Certificates and dynamic blog template experiences added.

### Session 14-03-2026 12:00 - Site Polish and Content Corrections
- Activities section added.
- Favicon, sitemap, and robots introduced.
- Certificates metadata corrected.

### Session 14-03-2026 15:43 to 16:05 - Developer-Style Redesign Iterations
- Migrated hero and navigation toward developer-focused visual language.
- Enhanced mobile behavior (hero profile hidden on mobile), background atmosphere, and profile hover effects.
- Removed scroll cue when requested.
- Added hero cipher text system configured via `data/personal.json`.
- Added skill icons from external SVG providers and improved skill card visuals.

## Session 14-03-2026 16:08 - De-hardcode Skill Icons (Requested)

### User request
- Remove icon hardcoding from `index.html`.
- Keep icon data in JSON so future updates do not require touching HTML/JS logic.

### Changes made
- Updated `data/skills.json`:
  - Added `icon_url` to every skill entry across all categories.
  - All icon URLs now point to external provider SVGs (Devicon via jsDelivr).
- Updated `index.html`:
  - Removed hardcoded `SKILL_ICON_URLS` mapping object.
  - Removed `getSkillIconUrl()` hardcoded lookup logic.
  - Skills renderer now reads `s.icon_url` directly from JSON.

### Validation
- Reloaded `http://127.0.0.1:5500/index.html`.
- Confirmed no runtime errors and icons continue to render in the skills section using JSON-defined URLs.

---

This file is the latest cumulative memory snapshot. Future sessions should append new progress by creating the next timestamped `.compact_DDMMYYYYHHMMSS.md` with all prior history plus new updates.
